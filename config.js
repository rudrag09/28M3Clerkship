export const config = {
  supabaseUrl: 'YOUR_SUPABASE_URL',
  supabaseAnonKey: 'YOUR_SUPABASE_ANON_KEY',
  allowedEmailDomains: ['wright.edu'],
  appName: 'Clerkship Overlap',
  redirectTo: window.location.origin + window.location.pathname,
};
